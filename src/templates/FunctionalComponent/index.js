/* @flow */
import * as React from 'react';
import { Text, View } from 'react-native';

type Props = {};

export default function COMPONENT_NAME(props: Props): React.Element<any> {
  return (
    <View>
      <Text>This is the COMPONENT_NAME component!</Text>
    </View>
  );
}
